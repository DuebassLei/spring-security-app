server:
  port: 30016

spring:
  thymeleaf:
    cache: false # 关闭缓存
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/study?characterEncoding=utf8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform:  org.hibernate.dialect.MySQL5Dialect
    database: mysql